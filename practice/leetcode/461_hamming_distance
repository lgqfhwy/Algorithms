// https://leetcode-cn.com/problems/hamming-distance/
class Solution {
public:
    int hammingDistance(int x, int y) {
        int ret = 0;
        while (x || y) {
            ret += (x & 1) ^ (y & 1);
            x = x >> 1;
            y = y >> 1;
        }
        return ret;
    }
};